{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run npm tests",
            "type": "shell",
            "command": "npm",
            "args": [
                "test"
            ],
            "options": {
                "cwd": "${workspaceFolder}/backend"
            }
        }
    ]
}