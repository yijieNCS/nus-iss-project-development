export class Report {
    constructor(reportId, report, reportedUser, reportBy) {
        this.reportId = reportId
        this.report = report
        this.reportedUser = reportedUser
        this.reportBy = reportBy
    }
}